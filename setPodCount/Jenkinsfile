pipeline {
    agent { label 'ci-agent' } 
    stages {
        stage('scale web-server'){
            steps {
                container('kubectl'){
                   sh "kubectl scale --replicas=3=$podCount deployment/nginx-deployment -n web"
                }
            }
        }
    }
}